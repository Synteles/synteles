name: CI

on:
  pull_request:
  push:
    branches: [main]

# Minimal permissions — safe for fork PRs (no secrets needed here)
permissions:
  contents: read

jobs:
  # ── Python services ───────────────────────────────────────────────────────
  #
  # Runs lint + type-check + security scan + unit tests for each service.
  # fail-fast: false so all services report in one go rather than stopping
  # at the first failure — better feedback for contributors fixing multiple things.

  python:
    name: Python / ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - core-service
          - scheduler-service
          - synte-service
          - platform-db
          - durable-worker
    defaults:
      run:
        working-directory: ${{ matrix.service }}
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-caching: true
          cache-dependency-glob: "${{ matrix.service }}/uv.lock"

      - name: Install dependencies
        run: uv sync --group dev

      - name: Lint, type-check, security scan
        run: make check

      - name: Unit tests
        run: make test

  # ── Frontend ──────────────────────────────────────────────────────────────

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ux-console
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11.1.2

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: ux-console/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Unit tests
        run: pnpm test

      # Build before e2e: type-checks the codebase and produces the production
      # bundle that `pnpm start` (the e2e webServer) needs.
      - name: Build
        run: pnpm build
        env:
          REDIRECT_URI: http://localhost:3000/callback
          OIDC_ISSUER_URL: http://localhost:4444
          OIDC_CLIENT_ID: test-client
          OIDC_CLIENT_SECRET: test-secret
          API_BASE_URL: http://localhost:4444
          CHAT_STREAM_URL: http://localhost:4444

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E tests
        run: pnpm test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: ux-console/playwright-report/
          retention-days: 7
